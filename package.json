{
  "name": "sd-legal",
  "version": "0.1.0",
  "private": true,
  "description": "SD Legal — Escritório de Advocacia Digital",
  "scripts": {
    "dev:paperclip": "pnpm --filter paperclip dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
